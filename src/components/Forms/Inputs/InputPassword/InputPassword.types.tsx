export type InputPassword = {
    id: string,
    className?: string,
    name: string,
    placeholder?: string,
    pattern?: string,
    ariaLabel: string,
    value?: string,
    maxLength: number,
};